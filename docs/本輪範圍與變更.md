# MZSM｜2026-09-10 候選施工

任務：MZSM-CONTINUE-20260910-001。依 Owner 本次專案交接與後續「繼續」指示施工。

本次範圍：以取回的 83 檔修正版為來源，與 GitHub 程式交接分支比對；保留 15 頁、原主圖、Logo、書法、字型與煙霧素材。新建明確候選分支，不覆蓋 main，不連線 Supabase，不改 Hosting／DNS，不對外聯絡。

來源封包 SHA256：670546b49ac51f9ead686bb486b732ce5d4c8d2d5f341fc4d20585e71b2a84c5。

GitHub 父提交：1cf0d5f2ea73fa7bc3c640515d267d462b068d46。
封包記錄的隔離施工版本不是 GitHub 分支提交，不能拿來做 GitHub 交付證明。

## 實際變更

- 移入 9 月 8 日已驗證的 F01～F11 修正。
- 新增八表明確 RLS ENABLE、restrictive deny-direct policy，以及表級／欄位級權限撤回；繼承權限未能收斂則交易中止。
- 新增真實 DB 負向測試、唯讀前置盤點、公開查詢個資正向修補 migration 與函式目錄檢查。
- 新增空值 .env.example 與忽略真實 .env 的規則；未引入後端 secret 或虛構環境設定。
- 公開輸出移除尚無 Owner 可信來源的宮史、聯絡、活動、服務與個資收件區塊；不以 CSS 隱藏後把原文字留在公開 HTML。
- 新增無外部相依的公開內容處理及靜態交付檢查；保留來源 HTML 與既有素材，未整頁重做。
- RUN_ALL 生成器同步列入新 migrations，但 Owner 本輪應依 SQL-執行順序.md，不能用舊批次文件直接套用正式專案。

## 驗證與人工節點

Build PASS；Mock contract tests 57/57 PASS，mock 環境未連線真實後端；靜態交付檢查 PASS。完整原始結果見 docs/驗證原始結果。

DB migration、RLS negative DB test、RPC privacy catalog test、Browser E2E、Lighthouse、真機、減少動態實測、線上安全標頭、Preview、Staging、Production 一律 NOT RUN。

新分支發布後，依 Owner 補充第 23.2 條，停在 Supabase SQL Editor 人工執行節點，提供精確檔案順序，等待原始輸出。不能因 RLS SQL 已寫而宣稱真實資料庫安全，也不能因此直接開啟前端收件或 Production。

本輪未開放新網站。未指定 Hosting／正式網域／Supabase 專案／DNS Zone，不替 Owner 猜測。其餘發布驗收及美感比對頁尚未完成，後續接續。
